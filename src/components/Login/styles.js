import { makeStyles } from '@material-ui/core/styles';

export default makeStyles(() => ({
  container:{
    marginTop: '100px',
    width: '100%',
    display: 'flex',
    justifyContent: 'center',
  }
}));
